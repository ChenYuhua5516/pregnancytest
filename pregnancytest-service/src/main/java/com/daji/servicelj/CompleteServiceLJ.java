package com.daji.servicelj;

import com.daji.pojo.Complete;

public interface CompleteServiceLJ {
    Complete completecx(Complete complete);
    int Completexz(Complete complete);
    int Completesc(Complete complete);
}
